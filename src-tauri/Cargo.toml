[package]
name = "mojian"
version = "0.0.1"
description = "墨笺：以档案为中心的桌面笔记"
authors = ["NAMEWTA"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "mojian_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
