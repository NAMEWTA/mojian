{
  "identifier": "default",
  "description": "Main window: vault commands and native folder picker.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "vault"
  ]
}
